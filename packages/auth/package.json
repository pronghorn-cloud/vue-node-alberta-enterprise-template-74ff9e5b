{
  "name": "@template/auth",
  "version": "1.0.0",
  "description": "Authentication drivers (SAML, Entra ID, Mock) for Alberta Government enterprise template",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@template/shared": "*",
    "@template/config": "*",
    "@node-saml/passport-saml": "^5.1.0",
    "openid-client": "^6.8.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}